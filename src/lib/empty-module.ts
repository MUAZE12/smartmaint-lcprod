// Empty stub — targeted by Turbopack's resolveAlias for
// onnxruntime-node so the Node.js ONNX runtime never lands in the
// browser bundle. Webpack uses `false` (its own convention) via
// next.config.ts webpack: hook.
export default {};
